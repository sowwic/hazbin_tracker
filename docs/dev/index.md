# Developer Guide

## Setup

Project is using UV and Python 3.12 as basic requirements.

1. Create a new venv and activate it
2. Run `uv sync --locked --all-extras --dev`
3. If you are using VsCode will launch new instance of the app.
